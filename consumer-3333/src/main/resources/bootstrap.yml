#spring:
#  cloud:
#    config:
#      name: consumer #需要从github上读取的资源名称，注意没有后缀名
#      profile: dev   #本次访问的配置项
#      label: master
#      uri: http://localhost:5555  #本微服务启动后先去找5555号config服务，通过SpringCloudConfig获取GitHub的服务地址
#
#
